name: CI

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Running custom action
      uses: ravshanovusmonbek/print_ci@v1
